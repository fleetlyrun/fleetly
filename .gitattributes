# proto 源强制 LF：注释里的换行会经 protoc-gen-openapiv2 原样嵌入 swagger
# title；Windows 检出的 CRLF proto 会生成含 \r\n 的生成物，CI（Linux）再生成
# 即漂移（T2-9b proto 门翻车实录）。生成物由 proto 派生，源头钉 LF 即全局确定。
*.proto text eol=lf
# 生成物同样钉 LF：保证 Windows 本机再生成与 CI（Linux）再生成逐字节一致，
# 生成物同步门（proto job 的 git diff --exit-code）不再依赖 autocrlf 归一。
genproto/** text eol=lf
# dind/POSIX 执行面脚本同样钉 LF（宿主编排已带 CR 剥离兜底，此处为仓库侧一致性）。
*.sh text eol=lf
# PEM 密钥/公钥同样钉 LF：openssl 的 PEM 解析不吃 CRLF，且 release.yml 的
# embedded-pubkey gate 与 test-install.sh A11 对公钥块做逐字节比对——
# Windows 检出转 CRLF 会同时破坏签名与一致性断言。
*.pem text eol=lf
# 测试 golden 快照同样钉 LF：CLI golden 与 got 逐字节比较、migrations.golden
# 对迁移 SQL 原始字节做 sha256——Windows 检出转 CRLF 会让本机测试误报漂移
# （CI Linux 不受影响，但本地全绿与 CI 必须同判据）。
*.golden text eol=lf
# 迁移 SQL 钉 LF：migrations_test 对已应用文件原始字节 sha256（只加法纪律），
# CRLF 检出会使全部哈希漂移；与 proto/genproto 同款「源头钉 LF」策略。
*.sql text eol=lf
# HTML 钉 LF（当前仅 console/index.html）：内联防闪主题脚本的原始字节被
# consoleCSP（internal/runtime/console_static.go）以 sha256-'…' 静态哈希
# 白名单放行——行尾随 autocrlf/检出平台漂移会改变脚本字节使哈希失配、
# CSP 静默拦截脚本（防闪失效回归）。源头钉 LF 即哈希全局确定（2026-09-25
# 走查修复；同款「源头钉 LF」策略）。
*.html text eol=lf
