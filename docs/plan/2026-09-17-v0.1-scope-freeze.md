# fleetly v0.1 实现切面冻结清单（T0.5）

| 状态 | 日期 | 关联 |
|---|---|---|
| 已冻结 | 2026-09-17 | 架构 §4.5 要求的「三专项 v0.1 切面重算并冻结」载体；[任务分解](2026-09-17-task-breakdown.md) T0.5 产出；对 [发布专项](../design/2026-09-17-release-semantics.md)、[放置专项](../design/2026-09-17-stateful-placement.md)、[状态模型专项](../design/2026-09-17-state-model.md) 的 v0.1 条目逐项裁决；变更本清单须显式评审（等同错误码只增纪律） |

## 1. 裁决（本清单新增的口径，不是复述）

| # | 裁决 | 理由 |
|---|---|---|
| FZ-1 | **「自动连接串」后置 v0.2**：v0.1 第 4 项 env/密钥交付 = 加密存储 + 注入 + 三层合并，无连接串生成（v0.1 无数据服务可连；连接串随 v0.2 数据库模板落地，以 `source=system` 平台 env 形态注入） | 架构 §4.2 第 4 项的遗留歧义；v0.1 不留空机制 |
| FZ-2 | **退化信封口径**：无 ErrorResponse detail 的错误（框架/传输层）经 gateway 输出 `code:""` + message 保底的通用信封，HTTP 状态由 grpc code 机械映射——不发明清单外码、不挪用既有码语义 | 阶段 3 遗留①裁决：接受实现取舍，冻为 v0.1 口径 |
| FZ-3 | **未定 HTTP 映射的 15 个错误码冻结如下表缺省值**（文档显式给定的 9 个维持：`E_STATE_VERSION_CONFLICT`=409、`E_LABEL_RESERVED`=422、`E_DOMAIN_CONFLICT`=409、`E_PLACEMENT_MOVE_REQUIRES_ACK`=409、`E_VOLUME_NODE_MISMATCH`=409、`E_PLACEMENT_LABEL_CONFLICT`=422、`E_PLACEMENT_NODE_INVALID`=422、`E_PLACEMENT_NODE_NOT_FOUND`=422、`E_EVENT_CURSOR_EXPIRED`=410） | 阶段 3 遗留②裁决；调整任一映射 = 契约变更，须评审 |
| FZ-4 | **cron 超时事件命名 `cron.timed_out`**（与 `cron.skipped` 过去分词风格一致；按只增流程随后续 cron 实现入注册表，当前注册表 35 事件不含它属预期） | 阶段 3 遗留③裁决；架构 §4.3「超时→事件」的命名补全 |
| FZ-5 | **校验违规（protovalidate）信封**：`code:""`（InvalidArgument）+ 违规字段路径进 `context`——不新增 `E_REQUEST_INVALID` 类通用码；v0.1 用户可见的同步校验错误集中在 compose 解析层（`E_COMPOSE_*`） | 与 FZ-2 同源：清单外不造码 |

### FZ-3 缺省 HTTP 映射表（15 码）

| 码 | HTTP | 语义依据 |
|---|---|---|
| E_COMPOSE_UNSUPPORTED / E_COMPOSE_MANAGED_FIELD / E_COMPOSE_UNSAFE_STRATEGY | 400 | 同步校验拒绝（apply 前失败，发布专项 §2.5 场景 1） |
| E_CAPABILITY_REQUIRES_MULTI_NODE | 400 | 单节点误用多节点操作（放置 §2.9） |
| E_PLACEMENT_NODE_UNAVAILABLE / E_PLACEMENT_NODE_GONE / E_PLACEMENT_NO_ELIGIBLE_NODE | 409 | 部署前哨阻塞（快速失败，放置 §2.5） |
| E_ROLLBACK_NO_TARGET | 409 | 目标越界（发布专项 §2.4） |
| E_BACKUP_KEY_MISSING | 500 | 恢复流程系统错误（状态模型 §2.7） |
| E_BUILD_FAILED / E_IMAGE_PULL_FAILED / E_IMAGE_UNAVAILABLE / E_SCHEDULER_PENDING_TIMEOUT / E_TASK_START_FAILED / E_HEALTH_TIMEOUT / E_OBSERVE_CRASH_LOOP / E_OBSERVE_UNHEALTHY / E_DEPLOY_INTERRUPTED / E_DEPLOY_POST_WINDOW_UNSTABLE / E_DEPLOY_DOWNTIME_FAILED / E_ROLLBACK_FAILED / E_RUNTIME_UNAVAILABLE | 503（E_RUNTIME_UNAVAILABLE）/ 500（其余） | 管线执行类；注意「部署失败是资源终态而非 HTTP 错误」——这些码主要出现在 deployment 资源与事件流，HTTP 面仅在同步触发（如 preflight 立即失败）时呈现 |

## 2. 三专项 v0.1 实现切面（冻结）

### 2.1 发布专项（release-semantics §4 v0.1 条目核对）

| 切面 | v0.1 | 冻结说明 |
|---|---|---|
| pause 固定 + 错误码/事件 | ✅ | 错误码 37 码全量入注册表（阶段 3），事件 35 个全量入表（`cron.timed_out` 除外，见 FZ-4） |
| compose 受管字段校验 | ✅ | `E_COMPOSE_MANAGED_FIELD`/`E_COMPOSE_UNSAFE_STRATEGY` |
| 归一化 compose 快照 + 最近 5 版回滚 | ✅ | 快照含 **env 三层合并结果**（架构 §2.4 变量合并行） |
| 首发失败 scale=0 | ✅ | `deployment.substrate_halted` |
| 控制面重启分类恢复 | ✅ | 含 `blocked_waiting` 子状态（单节点 v0.1 不会触发场景 15/16，但状态机分支一并实现——单机同路径纪律） |
| `W_ROLLBACK_IMAGE_RISK` | ✅ | 已入表 |
| **后置**：v0.2 项（MCP 只读查询、degraded 一等 UI、多副本水位粗判） | ⛔ | 不进 v0.1 |

### 2.2 放置专项（stateful-placement §4 v0.1 条目核对）

| 切面 | v0.1 | 冻结说明 |
|---|---|---|
| `fleetly.placement.node` label 解析与校验 | ✅ | 422 + 候选清单 |
| 自动绑定（单节点同路径） | ✅ | 候选集唯一项 = 本机；平台节点 ID 体系 v0.1 即建 |
| 卷注册表（volumes 表） | ✅ | 含孤儿状态位 |
| 前哨 409 | ✅ | 单节点 v0.1 数据不匹配场景不可达，代码路径保留（同路径纪律） |
| 删除应用保留卷 | ✅ | orphaned 可见 |
| 多节点操作拒绝 | ✅ | `E_CAPABILITY_REQUIRES_MULTI_NODE` |
| **后置**：v0.2 项（多节点绑定与选点、GET /v1/nodes、孤儿清理指引、restic 迁移 + rebind CLI） | ⛔ | |

### 2.3 状态模型专项（state-model §4 v0.1 条目核对）

| 切面 | v0.1 | 冻结说明 |
|---|---|---|
| 三层原则（权威/缓存/直读） | ✅ | 写前直读 `E_STATE_VERSION_CONFLICT` |
| 观测缓存（单机同路径） | ✅ | `observed_at/stale`；nodes 表 v0.1 即有 |
| 最小 label 集 | ✅ | managed/app/process/deployment + node-id；命名约定（服务/secret/卷）适配器内 |
| `revisions` 表 | ✅ | 与发布切面共用 |
| tombstone 删除 | ✅ | 不复活测试 |
| `state_backups` 台账（热备 + sha256 回读 + 失败红色告警） | ✅ | 备份目标本地路径（S3 上传 v0.2） |
| 审计 fail-closed | ✅ | 与业务写同事务 |
| 事件 seq/游标 | ✅ | `E_EVENT_CURSOR_EXPIRED` 410 |
| **后置**：v0.2 项（完整 resync 刷新器、orphans 表、cron_runs、L1/L2 恢复演练、导出 tar、raft 冷备 helper、多节点 nodes 接入） | ⛔ | |

## 3. 范围外提醒（防蔓延）

- v0.1 只做单节点；多节点语义（场景 15/16、HA 边界、zot）一律 v0.2。
- 注册表码集 = 设计文档全量（实现面按需消费）；新增码走「只增 + 评审」。
- 本清单与任务分解冲突时，以本清单为准（更新须同步任务分解）。

## 4. 完成回填（随实现更新）

| 票据 | 状态 | 还原点 |
|---|---|---|
| T0.1 仓库与 CI 骨架 | ✅ 2026-09-17 | d8328d5 |
| T0.2 错误码与事件注册表 | ✅ 2026-09-17 | 421a991 |
| T0.3 proto 契约与生成链 | ✅ 2026-09-17 | 0a25e25 |
| T0.4 dind E2E 骨架 | ✅ 2026-09-17 | bfa0f48 |
| T0.5 切面冻结（本文档） | ✅ 2026-09-17 | 见本清单 commit |
| T1.1-T1.3 Spike A/B/C | ✅ 2026-09-17 | FINDINGS 落档 spike/*/README.md |
| T1.4 结论落档 + V1-V7 回归种子 | ✅ 2026-09-17 | e2e/nightly/（V5 裁决见 FZ-6） |
| T2.2-T2.14（状态层/构建/发布/回滚/漂移/放置） | ✅ 2026-09-18 | 8a0e6c5 / 2e2312b / 57a5c98 及此前还原点 |
| T2.15-T2.16（Traefik 入口 + ACME） | ✅ 2026-09-18 | 57a5c98 |
| T2.17+T2.20（API 面 + 日志管线） | ✅ 2026-09-18 | f60e52a |
| T2.18（CLI-over-SDK） | ✅ 2026-09-19 | 4ecf97d |
| T2.19（git push + webhook） | ✅ 2026-09-19 | 30cdc26 |
| T2.21（Console 端） | ✅ 2026-09-19 | 1d2166a |
| T2.1（安装器与引擎门禁，补票见 FZ-10） | ✅ 2026-09-19 | 62697a3 |
| T2.22+T2.23（备份基线 + 自升级双轨） | ✅ 2026-09-19 | 69299f0 |
| T2.24（交付流水线 M1） | ✅ 2026-09-19 | 683709e（+c204b48/00c0e8c 修复） |
| T2.25+T2.26（资源校准 + 端到端验收） | ✅ 2026-09-19 | 本清单同批 commit；[校准报告](../reports/2026-09-19-resource-calibration.md) / [验收记录](../reports/2026-09-19-v0.1-acceptance.md) |

## 5. 冻结轮确认（2026-09-19，v0.1 实现完成时）

**注册表增项确认（实现期新增、本轮冻结）**：

- 错误码 `E_ROUTE_PUBLISH_FAILED`（HTTP 503）——T2.15 路由发布失败告警语义（architecture §2.5），入表于 57a5c98，本轮确认冻结；v0.1 注册表终态 33 码（设计全量口径，实现面按需消费）。
- 事件 `route.published` / `route.publish_failed`——T2.15 路由发布生命周期，同批入表，本轮确认冻结；cron.timed_out 仍按 FZ-4 留待 cron 实现入表。
- 其余阶段零增码零增事件（T2.18/T2.19/T2.21/T2.22/T2.23 复用既有码族与审计面）。

**实现期裁决（续 FZ 编号，等同冻结纪律）**：

| # | 裁决 | 理由/出处 |
|---|---|---|
| FZ-6 | **V5/V5b（单 manager 故障 + --force-new-cluster 恢复）不进 CI**：nightly 保留 `if: false` 占位 job 与转译注记（spike/c README §5/§6） | T1.4 落档：宿主 docker 依赖编排 + 25 分钟连续性采样，CI 化编排成本最高且转译要点已留档；真路径归 release VPS 验证 |
| FZ-7 | **ObjectStore conformance 记 N/A（v0.1）**：minio-go 在代码库零使用、无真实消费者，MinIO/RustFS 代演无验证对象 | T2.24 实现期裁决；随 v0.2 备份上传统一落地（届时 RustFS 作 CI 代演） |
| FZ-8 | **验签形态 = cosign keyless bundle**：checksums.txt.sig 为 sign-blob --bundle 自足产物；安装器/升级器 verify 用 `--bundle`（cosign ≥2.6 三选一硬门） | T2.24/T2-9c 验收修复；identity 约束 = OIDC issuer token.actions.githubusercontent.com + repo regexp |
| FZ-9 | **资源预算口径修订**：containerd（≈44-50MB）单列并入 v0.2 总量预算；dockerd 水位 ≈110MB + 2.4MB×应用数；fleetlyd+dockerd ≈160.7MB 达标维持 <200MB | [T2.25 校准报告](../reports/2026-09-19-resource-calibration.md)；架构 §4.2 已同步 |
| FZ-10 | **T2.1 为排期补票**（原 T2 排班遗漏安装器，T2-9a 补做）；安装目录约定 `/opt/fleetly`（bin/etc）+ `/var/lib/fleetly`（数据根 0700）+ `/usr/local/bin` 符号链接 | T2-9a 实现期发现与裁决 |
| FZ-11 | **CLI 语义面**：CLI 只经 SDK（gRPC）消费平台（--db/--key/--docker-host 直连路径全部移除）；flag 必须先于位置参数（lynx-go/commands 约定）；退出码 0/1/2（2=plan/diff 有变化或用法错误） | T2.18 落地口径；双 DB divergence 根治 |
| FZ-12 | **SSH 拉源 TOFU 裁决（2026-09-19 对抗审查轮）**：v0.1 维持 `StrictHostKeyChecking=accept-new`（单操作员口径：admin 本可替换 known_hosts，钉 host key 基础设施 v0.2 随多用户引入）；缓解已落地——known_hosts 钉位 `<git.root>/known_hosts`，**首连指纹落审计**（`git.hostkey_first_seen`，带 host+指纹），异常指纹增长可审计追溯 | internal/gitserver/fetch.go 实现注释原自认「待冻结轮复核」而冻结轮未复核——本轮补裁；同时拉源 URL 加协议白名单（https/http/ssh/git@/file 有意保留，ext::/fd:: 禁用） |
