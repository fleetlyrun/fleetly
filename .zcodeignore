# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Dependency directories
/vendor/

# go.work / go.work.sum 自 T0.3 起提交入库（三 module：主仓 + genproto +
# sdk/go）；勿在此忽略。

# Built binaries at repo root
/fleetlyd
/fleetly.db
/fleetly.db-shm
/fleetly.db-wal
/fleetly.key
/fleetly

# Local config product (config-example.yaml is the committed template)
/config.yaml

# Editors / OS
.idea/
.vscode/
.DS_Store

# Spike A artifacts: plan json 与 logs 保留入库；大文件（二进制/传输载荷）排除
spike/a/artifacts/spikerailpack-linux
spike/a/artifacts/spikerailpack-linux.b64
spike/a/artifacts/spikerailpack.exe
spike/a/artifacts/*.tar
spike/a/artifacts/*.b64 
# Spike B artifacts: logs ������⣻�������̽�����������ļ��ų� 
spike/b/dockerctx/probe 
spike/b/artifacts/*.tar 
spike/b/artifacts/*.b64
 
# Spike C artifacts: logs are kept in repo; probe binary and swarm backups (contain private keys) excluded 
spike/c/dockerctx/probe 
spike/c/artifacts/hostbak/ 
spike/c/artifacts/*.tar 
spike/c/artifacts/*.b64 

# Console (T2.21): deps and build output are reproducible from the lockfile;
# pnpm-lock.yaml IS committed.
console/node_modules/
console/dist/

# ===== ↑ 以上同步自 .gitignore（「从 .gitignore 同步」只重写以上部分）=====
.git/
.hg/
.svn/
node_modules/
bower_components/
jspm_packages/
__pycache__/
site-packages/
venv/
coverage/
htmlcov/
lcov-report/
cmakefiles/
cmake-build-*/
bazel-*/
pods/
deriveddata/
storybook-static/
playwright-report/
test-results/
allure-results/
allure-report/
cdk.out/
*.egg-info/
*.dist-info/
eggs/
pip-wheel-metadata/
wheels/
# ----- ↑ 以上为 ZCode 默认排除规则（自定义规则请写在本行下方，不会被同步/恢复改动）-----
# 自定义规则写在下方（本行提示可删除）
